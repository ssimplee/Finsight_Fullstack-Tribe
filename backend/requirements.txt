fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
python-multipart==0.0.20

# Member 4 RAG deps (optional: backend falls back to mock if absent)
chromadb>=0.5.0
sentence-transformers>=2.2.0
